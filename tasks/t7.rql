?inp1 = "foo.ttl" .
?inp2 = "normFoo.ttl" .
?t7graph = init ?inp1 ?inp2 .

SELECT (?s, ?p, ?o) FROM (?t7graph) WHERE {
    ((?s = ^<http://example.org/jane>) && 
    (!(?p = ^<http://ont.example.org/hasName>)) && 
    (!(?o = "Fred" || ?o = "Sally"))) ||
    ((!(?s = ^<http://example.org/jane>)) && 
    (?p = ^<http://ont.example.org/hasName>) && 
    (!(?o = "Fred" || ?o = "Sally"))) ||
    ((!(?s = ^<http://example.org/jane>)) && 
    (!(?p = ^<http://ont.example.org/hasName>)) && 
    (?o = "Fred" || ?o = "Sally")) .
}